-- V10__noop_migration.sql
-- Flyway version 10 already exists in the database schema.
-- This is a no-op migration to align the codebase with the database schema version.
-- No schema changes needed - this migration exists to satisfy Flyway's version check.

SELECT 1;